<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/leaf"
        app:layout_constraintTop_toTopOf="parent"
        app:title="@string/tv_wanna_help"
        app:titleCentered="true"
        app:titleTextAppearance="@style/TextStyle2" />

    <TextView
        android:id="@+id/tv_choose_help_category"
        android:layout_marginTop="@dimen/spacing_l"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/TextStyle5"
        android:gravity="center"
        android:text="@string/tv_title_choose_category"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_categories"
        android:layout_marginTop="@dimen/spacing_l"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        app:layout_constraintTop_toBottomOf="@id/tv_choose_help_category"
        />

    <include
        android:id="@+id/bottom_nav_view"

        layout="@layout/bottom_nav_view_layout" />

</androidx.constraintlayout.widget.ConstraintLayout>